import { createStyles } from '@mantine/core';

export default createStyles ((theme) => ({
        styles21: {
        borderBottom: 'none'
      },

      inner: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center',
      },
})
)